# Deprecated Stripe consumer functions

Per-function Appwrite deployments that predated **`../stripe-consumer`**. Kept for reference or gradual cutover; new work should target **`functions/stripe/stripe-consumer`** only.

The former shared **`lib/`** helpers (`callStripeGateway`, `appwriteEnv`, `mergedEnv`) live in **`deprecated/lib/`**; the active copy is **`../stripe-consumer/lib/`**.
