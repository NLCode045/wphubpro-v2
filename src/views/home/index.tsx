import { Container } from 'react-bootstrap'

/** Empty shell — topbar + sidebar only; add app content here. */
const HomePage = () => {
  return <Container fluid className="py-4" />
}

export default HomePage
